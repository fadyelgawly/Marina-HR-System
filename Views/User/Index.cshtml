@model IEnumerable<User>

<a class="btn btn-success" asp-controller="Account" asp-action="CreateUser">أدخل جديد</a>
<br/><br/><br/>
        <table class="table table-striped">
            <th>
                <td>الاسم</td>
                <td>Phone</td>
                <td>Department</td>
                
            </th>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.PhoneNumber</td>
                    <td>@item.DepartmentID</td>
                
                </tr>
            }
        </table>

